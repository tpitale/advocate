<div class="container-center">

  <p class="interaction-section-header">Open</p>
  <ul class="interactions-container">
    <% @interactions.each do |interaction| %>
      <%= render interaction %>
    <% end %>
  </ul>
</div>
