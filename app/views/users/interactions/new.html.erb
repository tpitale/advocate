<%= form_for [:users, @interaction], html: {class: "simple-form"} do |f| %>
  <%= f.label :phone, "Client Phone #" %>
  <%= f.text_field :phone %>

  <%= f.submit "Create", class: "c-btn" %>
<% end %>
